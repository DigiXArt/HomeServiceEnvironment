{
	"name": "Service Router Service",
	"dockerFile": "../Dockerfile",
	"runArgs": [ "--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined", "--expose=6000" ],

	// Use 'appPort' to create a container with publ