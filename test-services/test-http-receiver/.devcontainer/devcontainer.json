{
	"name": "Test HTTP Receiver Service",
	"dockerFile": "../Dockerfile",
	"runArgs": [ "--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined", "--expose=8080" ],

	// Use 'appPort' to create a container with publish